// Fluidwind - Tailwind CSS plugin for CSS clamp()-based fluid scaling
// Public API: default export is the plugin, named exports are types

export { default } from "./plugin";
export type {
  FluidWindConfig,
  FluidWindOptions,
  ParsedFluidValue,
  ParsedFluidColor,
  ParsedNumber,
  RGBColor,
} from "./types";
